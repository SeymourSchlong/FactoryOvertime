meta:
targets = 'gml_GlobalScript_scr_Load_AutoSave'
critical = true

patch:
find_line_with('global.Money = clamp(global.Money, 0, obj_LvlMGMT.MaxMoney)')

write_replace('
global.Money = clamp(global.Money, -obj_LvlMGMT.MaxMoney, obj_LvlMGMT.MaxMoney);
')