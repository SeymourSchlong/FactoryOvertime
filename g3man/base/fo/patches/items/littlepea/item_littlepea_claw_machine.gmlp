meta:
targets = 'gml_Object_obj_CMPlayBtn_Step_0'
critical = true

patch:
find_line_with('global.Money -= CMPrice')

write_before('
if (global.fo.littlepeas > 0)
{
    var _coins_to_give = floor(CMPrice / 2);
    nnf_give_coins(_coins_to_give);
    
    global.fo.littlepeas -= 1;
}
')